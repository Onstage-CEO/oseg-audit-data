# Downstream Readiness Lock

Until The Phoenician human editorial attestation passes:

- Caesars Republic must not be regenerated or changed.
- International properties must not be regenerated or changed.
- Every mutating production workflow must pass the shared Phoenician attestation freeze gate before its first mutating job. This includes scaffold, capture, imports, drains, briefs, reports, entertainment evidence, and status commits.
- The only pre-attestation exception is `artifact_only` generation for `the-phoenician-resort-and-spa-rebuilt-2026-07-31`; it uploads a preview and commits nothing.
- Phoenician `publishMode: commit` is disabled. The canonical report may be changed only by promoting the exact hash-attested artifact, never by regenerating model-authored HTML after review.
- Source capture, provider spending, deployment, publication, and canonical-package replacement remain unauthorized.

The lock is released only by the hash-bound attestation path; it cannot be bypassed by a readiness override.
